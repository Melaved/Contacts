<UserControl x:Class="View.Controls.ContactControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:View.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="300">
    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <converters:InverseBooleanConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.BindingGroup>
            <BindingGroup x:Name="ContactBindingGroup"/>
        </Grid.BindingGroup>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Изображение -->
        <Image
            Grid.Row="0"
            Grid.RowSpan="6"
            Grid.Column="0"
            Width="100"
            Height="100"
            Margin="0 20 8 0"
            VerticalAlignment="Top"
            Source="\Resources\photo_placeholder_100x100.png"/>

        <!-- Поля для ввода данных -->
        <!-- Name -->
        <TextBlock
            Grid.Row="0"
            Grid.Column="1"
            VerticalAlignment="Center"
            Text="Name:"/>
        <TextBox
            Grid.Row="1"
            Grid.Column="1"
            Margin="3"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            IsReadOnly="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}"
            Text="{Binding Name, UpdateSourceTrigger=Explicit}"/>

        <!-- Phone Number -->
        <TextBlock
            Grid.Row="2"
            Grid.Column="1"
            VerticalAlignment="Center"
            Text="Phone Number:"/>
        <TextBox
            Grid.Row="3"
            Grid.Column="1"
            Margin="3"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            IsReadOnly="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}"
            Text="{Binding PhoneNumber, UpdateSourceTrigger=Explicit}"/>

        <!-- Email -->
        <TextBlock
            Grid.Row="4"
            Grid.Column="1"
            VerticalAlignment="Center"
            Text="Email:"/>
        <TextBox
            Grid.Row="5"
            Grid.Column="1"
            Margin="3"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Center"
            IsReadOnly="{Binding IsEditMode, Converter={StaticResource InverseBooleanConverter}}"
            Text="{Binding Email, UpdateSourceTrigger=Explicit}"/>

        <!-- Кнопка Apply -->
        <Button
            Grid.Row="6"
            Grid.Column="1"
            Width="75"
            Height="25"
            Margin="0 20 0 0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            Command="{Binding DataContext.ApplyCommand, RelativeSource={RelativeSource AncestorType=Window}}"
            CommandParameter="{Binding ElementName=ContactBindingGroup}"
            Content="Apply"
            Visibility="{Binding DataContext.IsApplyButtonVisible, RelativeSource={RelativeSource AncestorType=Window}, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>