<Window
    x:Class="View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:View"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Contacts"
    Width="525"
    Height="375"
    Padding="15"
    Icon="\Resources\ContactsApp_96x96.ico"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!--  Левая панель  -->
        <Grid Grid.Column="0" Margin="8">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!--  Поле поиска  -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Margin="0 0 5 0"
                    VerticalAlignment="Center"
                    Text="Find:"/>
                <TextBox
                    Margin="30,0,0,0"
                    VerticalAlignment="Center" Grid.ColumnSpan="2"/>
            </Grid>

            <!--  Список контактов  -->
            <ListView
                Grid.Row="1"
                Margin="0,8,0,8"
                Background="#f8f8f8"
                ItemsSource="{Binding Contact}"
                SelectedItem="{Binding SelectedContact, Mode=TwoWay}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Margin="3" Text="{Binding Name}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!--  Кнопки управления  -->
            <!-- Кнопки управления -->
            <Grid Grid.Row="2" Margin="3 3 15 3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Height="25" Margin="3" Content="Add"/>
                <Button Grid.Column="1" Height="25" Margin="3" Content="Edit"/>
                <Button Grid.Column="2" Height="25" Margin="3,3,0,3" Content="Remove"/>
            </Grid>

        </Grid>

        <!--  GridSplitter  -->
        <GridSplitter
            Grid.Column="1"
            Width="2"
            HorizontalAlignment="Center"
            VerticalAlignment="Stretch"
            Background="Gray"/>

        <!--  Правая панель  -->
        <Grid Grid.Column="2" Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--  Изображение  -->
            <Image
                Grid.Row="0"
                Grid.RowSpan="6"
                Grid.Column="0"
                Width="100"
                Height="100"
                Margin="0 20 8 0"
                VerticalAlignment="Top"
                Source="\Resources\photo_placeholder_100x100.png"/>

            <!--  Поля для ввода данных  -->
            <TextBlock
                Grid.Row="0"
                Grid.Column="1"
                VerticalAlignment="Center"
                Text="Name:"/>
            <TextBox
                Grid.Row="1"
                Grid.Column="1"
                Margin="3"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Text="{Binding SelectedContact.Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock
                Grid.Row="2"
                Grid.Column="1"
                VerticalAlignment="Center"
                Text="Phone Number:"/>
            <TextBox
                Grid.Row="3"
                Grid.Column="1"
                Margin="3"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Text="{Binding SelectedContact.PhoneNumber, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock
                Grid.Row="4"
                Grid.Column="1"
                VerticalAlignment="Center"
                Text="Email:"/>
            <TextBox
                Grid.Row="5"
                Grid.Column="1"
                Margin="3"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Text="{Binding SelectedContact.Email, UpdateSourceTrigger=PropertyChanged}"/>

            <!--  Кнопка Apply  -->
            <Button
                Grid.Row="6"
                Grid.Column="1"
                Width="75"
                Height="25"
                Margin="0 50 0 0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Content="Apply"/>
        </Grid>
    </Grid>
</Window>